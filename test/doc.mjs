// Writes docs/email-templates.md from src/mail/mail.txt, section for section, verbatim. `node test/doc.mjs`.
import { readFileSync, writeFileSync } from 'node:fs';
const pack = readFileSync('src/mail/mail.txt', 'utf8'), parts = pack.split(/^### (\S+)\r?\n/m).slice(1);
const who = { 'kickoff-receipt': 'Scheduler → the organiser, privately, right after the kickoff', refused: 'Scheduler → the organiser when a limit is hit; nothing else goes out', ask: 'Scheduler → each person, privately; Reply-To is the Scheduler', receipt: 'Scheduler → the person who replied; the three inserts are empty unless needed', 'read-block': 'inserted into the receipt when the times came from a screenshot', 'unread-block': 'inserted into the receipt when some lines could not be read', 'replyall-line': 'prepended to the receipt when the reply also went to other people', 'unread-all': 'Scheduler → the person who replied when nothing could be read; nothing is stored', 'already-set': 'Scheduler → anyone who replies after the invite has gone', nudge: 'Scheduler → each silent person, once, after nudge_after_hours', invite: 'Scheduler → everyone, in the thread, invite.ics attached', 'no-overlap': 'Scheduler → the organiser only', unsure: 'Scheduler → the organiser only, when the chosen slot fails the gate in src/check.ts; nothing is sent to the group', 'nothing-here': 'Scheduler → a person whose direct reply carried no lines at all', 'none-receipt': 'Scheduler → the person who said none; the poll stays open for a correction', 'none-noted': 'Scheduler → the organiser, once per person who said none', 'unknown-sender': 'Scheduler → the organiser, once per unmatched address that wrote to the poll', 'nudge-status': 'Scheduler → the organiser with the nudge round: who is still missing', 'dropped-block': 'inserted into a receipt when a line was read but fell outside the days, hours or window, with the reason', closed: 'Scheduler → a person who replies after the window passed', 'window-passed': 'Scheduler → the organiser when the window ends, or the poll expires, without an invite', 'shot-option': 'the third option in the ask, present only when the screenshot reader is on', 'shot-line': 'the screenshot sentence in the nudge and unread receipts, present only when the screenshot reader is on', vision: 'the prompt sent with a screenshot to the model; not an email' };
let out = `# Email templates\n\nGenerated from \`src/mail/mail.txt\` by \`node test/doc.mjs\`; each fenced body is that file's section, verbatim.\n\`{{placeholders}}\` are filled from the meeting. Every email is a reply in the same thread: subject \`Re: <meeting name>\`,\nsender \`Beargrass Scheduler for <organiser>\` at \`meet+<id>@\`, Reply-To the same. \`{{ex1}}\`–\`{{ex3}}\` are the first three working days of the window.\n\n## §0 The kickoff (the organiser sends; not a Scheduler template)\n\nTo: the people. CC: \`meet@beargrass.ai\`. Subject: the meeting's name. The four labeled lines are optional.\n\n\`\`\`\n${readFileSync('test/fixtures/kickoff.eml', 'utf8').split(/\r?\n\r?\n/).slice(1).join('\n\n').trim()}\n\`\`\`\n`;
for (let i = 0; i < parts.length; i += 2) out += `\n---\n\n## §${i / 2 + 1} \`${parts[i]}\` — ${who[parts[i]] ?? ''}\n\n\`\`\`\n${parts[i + 1].replace(/\n$/, '')}\n\`\`\`\n`;
writeFileSync('docs/email-templates.md', out);
console.log(`docs/email-templates.md: ${parts.length / 2} sections`);
